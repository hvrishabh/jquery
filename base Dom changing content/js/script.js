$(function () {
  //text(), html()

  // var firstPara = $("p:first");
  // console.log(firstPara.text());
  // console.log(firstPara.html());

  // firstPara.text("<strong>Hello</strong>")
  // firstPara.html("<strong>Hello</strong>")


  // var firstPara = $("p:first");
  // firstPara.append("   this is just append text")  // here this is added but creates a child element.

  // var firstPara = $("p:first");
  // firstPara.html(firstPara.html()+ " This was just appended text")   // this here only adds the text no extra chlid element is created here.

});

$(function () {
  // var emailInput = $("input[type='email']");
  // var textInput = $("input:text");
  // emailInput.val(textInput.val()+'@gmail.com');
  // console.log(textInput.val())
});
