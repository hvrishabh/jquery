$(function () {

  // $("li").remove();
  // $("form").children().not("input:text,textarea,br").remove();


  // var detachListItem = $("li").detach();
  // $('#content').append(detachListItem);

  $("p:first").empty();   // it removes all the child element and all the text inside the paragraph element.

  $(".test").empty();   // it has removed the text inside the function but not the complete function .

});
