$(function () {
  // $("li").replaceWith("<li>Replace</li>"); // if the main items are replaced with this, this means their sub-items will be gone.

  // $("li").replaceWith(function () {
  //   return "<li>Replace with function </li>";
  // });

  // var firstListItem = $("li:first");
  // $("p:first").replaceWith(firstListItem);

  // var greenBox = $("<div class='green-box'> More green box </div>");
  // $(".red-box , .blue-box").replaceWith(greenBox);

  // $("<div class='green-box'>more green </div>").replaceAll(".red-box, .blue-box")

});
